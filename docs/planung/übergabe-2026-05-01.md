# Übergabe Phase 2 · 2026-05-01

**Status: Konzept Career Dashboard abgeschlossen + Item 98 live + i18n-Defekte behoben + 2 Projektkonventionen verankert.**
aistrikemap.org läuft auf `b0daff9`. Sauberer Halt-Punkt für nächsten Session-Start.

---

## Was in dieser Session passiert ist (23 Commits, alle in `origin/main`)

| Block | SHAs | Inhalt |
|---|---|---|
| **Konzeptphase Career Dashboard (Items 94–99)** | `2295104`, `4b9a105`, `d5b2c6a`, `106db4d`, `7a318a3`, `7a2542c`, `7664219`, `82c3c03`, `35a4919`, `73243c8` | 10 Commits — alle 6 Punkte (Datenquellen, Score-Methodik, UX, Bias-Transparency, Recht, MVP-Scoping) entschieden und dokumentiert |
| **Item 98 — AI Labor Impact Ticker live** | `329086f` | `labor-impact.html`, `js/labor-impact.js`, `css/labor-impact.css`, `data/labor-impact-rates.json`, i18n DE/EN, Nav-Integration in 8 Pages, Sitemap-Eintrag |
| **Übergabe-Doc + Memory** | `24a86e1` | dieses Dokument (Vorgängerstand) |
| **2 Projektkonventionen verankert** | `9adefd7`, `a547f2c`, `9a433dd` | i18n-Vollabdeckung + Umlaute korrekt → CLAUDE.md; Audit-TODOs → pre-release-todos.md; Erst-Anwendungsfall: `uebergabe` → `übergabe`-Rename; Konvention-Präzisierung um technische Identifier |
| **i18n-Audit (statisch + dynamisch)** | `b9cd686`, `ec2410b`, `b1d9ab4`, `8a2237e`, `031743d`, `b0daff9` | 6 Commits — index.html instrumentiert; Nav-Pattern auf allen 8 Pages; radar.html ARIA; JS-Module-Audit; **Defekt-Fix in `js/detail-panel.js` und `js/map.js`** (i18n:changed-Listener) |

---

## Repo-State

- `main` = `b0daff9`, lokal + remote synced
- Branch `claude/jolly-tharp-34c888` lokal + remote noch da; Worktree-Ordner `.claude/worktrees/jolly-tharp-34c888/` physisch noch da → werden beim Session-Ende vom Harness aufgeräumt (Pattern aus Vorsession bekannt)

---

## Was das System jetzt zusätzlich kann

### 1. Konzeptphase Career Dashboard (Items 94–99) — 100 % entschieden

Sechs Quelldokumente unter `docs/planung/` bilden die vollständige Konzept-Basis:

1. `career-dashboard-datenquellen-matrix.md` — Top-20-Länder-Matrix, MVP = 8 API-Länder (DE/US/UK/FR/CA/NL/SE/AU)
2. `career-dashboard-score-methodik.md` — Bandbreiten-Modell aus 3 Schichten (national / OECD / Frey-Osborne) mit JSON-Schema
3. `career-dashboard-ux-konzept.md` — Map+Chart-Toggle, 5 ASCII-Wireframes
4. `career-dashboard-bias-transparency.md` — Fünf-Säulen-Modell, 8 i18n-Strings umsetzungs-ready
5. `career-dashboard-recht.md` — Lizenz-Spezifika pro Quelle, Standard-Disclaimer
6. `career-dashboard-mvp-scope.md` — 8-Schritt-Implementierungs-Plan, 10 Akzeptanz-Kriterien

Alle in `docs/planung/inventar-index.md` verlinkt.

### 2. Item 98 Labor Impact Ticker live unter aistrikemap.org/labor-impact.html

Worldometers-Style-Counter für KI-bedingte globale Job-Verdrängung/-Schaffung mit drei Quellen (WEF FoJ 2023, OECD Employment Outlook 2024, ILO World Employment 2024). Bandbreiten-Anzeige + Caveat-Box thematisiert Perspektiven-Divergenz. Konzept-konsistent mit Career Dashboard.

### 3. Internationalisierung strukturell behoben

Der ursprüngliche User-Eindruck "nur das Menü übersetzt" ist auf zwei Achsen adressiert:

**Statisch (alle 8 Pages):**
- Nav-Pattern (button + nav element ARIA) via `data-i18n-aria`
- index.html: 10 page-spezifische ARIA/Title-Hooks (filter-panel, search, severity, verification, detail-panel, map-legend etc.)
- radar.html: 3 page-spezifische ARIA-Hooks
- 13 neue i18n-Keys in `i18n/{de,en}.json`

**Dynamisch (IIFE-Module):**
- `js/detail-panel.js` Fix: `currentIncident`-State + `i18n:changed`-Listener → re-rendert offenes Panel bei Sprachwechsel
- `js/map.js` Fix: `lastIncidents`-State + `i18n:changed`-Listener → re-bindet Marker-Popups
- 5 von 8 Modulen hatten Listener bereits, 2 ergänzt, 1 hat keine Strings

### 4. Zwei Projektkonventionen in CLAUDE.md verankert

- **i18n-Vollabdeckung** ist Pflicht — Hardcoded German ist ein Bug. Jede Session liest CLAUDE.md.
- **Umlaute korrekt schreiben** (ä/ö/ü/ß), nicht ae/oe/ue/ss. Ausnahme: technische Identifier (URL-Slugs, JSON-Keys, i18n-Keys, CSS-Klassen, JS-Variablen).

---

## Offene Loose Ends

### Pre-Release vor finaler Veröffentlichung (siehe `docs/planung/pre-release-todos.md`)

1. **`data/labor-impact-rates.json` hat `"preliminary": true`** — Annualwerte gegen aktuelle Report-Versionen prüfen, bevor das Modul großflächig beworben wird.
2. **Body-Content-Restprüfung** der 4 statischen Pages (methodik, transparenz, impressum, datenschutz) — formal noch nicht durchgegangen, vermutet 95%+ ist bereits i18n'd.
3. **Umlaut-Vollaudit** über Bestandsdaten (Incident-JSON, ältere Konzept-Docs).
4. **`js/map.js` Restschwäche** — `addRadarMarkers()` rendert Status-ID direkt als Anzeige (sollte `I18n.t('radar.status.' + status)` sein), "Details →"-Link hardcoded. Niedrige Priorität (6 Marker).

### Architektur-Frage offen (PL-Frage an Projekteigner)

**Meta-Tags und `og:locale`** für ernste Internationalisierung — drei Optionen in `pre-release-todos.md` dokumentiert:
- (a) Status quo lassen (DE-Default-Page, Body via Browser switchbar)
- (b) Eigene `index.en.html` mit `<link rel="alternate" hreflang="en">` Cross-References — PL-Empfehlung für ernste SEO/Sharing-Internationalisierung
- (c) JS-gesteuerte Meta-Mutation — Crawler-wirkungslos

Brauche User-Entscheidung, bevor das in einen Implementierungs-Slot kommt.

### Worktree-Cleanup

`.claude/worktrees/jolly-tharp-34c888/` wird beim Session-Ende vom Harness aufgeräumt (analog Vorsession).

---

## Implementierungs-Roadmap (für nächste Session)

### Strategischer Default = Career Dashboard MVP starten

Aus `career-dashboard-mvp-scope.md` der 8-Schritt-Plan:

1. **Bundle-Script-Skelett** (`scripts/bundle-career-data.js`) — Schicht-Loader, Bandbreiten-Aggregator, Edge-Case-Klassifizierer (`robust`/`divergent`/`sparse`), Lizenz-Manifest. **Unit-Tests für Bandbreiten-Mathematik mandatorisch.** Methodisch der dickste Brocken — sollte mit vollem Token-Budget einer frischen Session begonnen werden.
2. Datenquellen-Anbindung pro Land (DE+US zuerst, höchste Datenqualität)
3. Beruf-Katalog finalisieren (~80 ISCO-4-digit-Codes aus Cluster-Plan in MVP-Doku)
4. `career.html` Skelett (analog `radar.html`-Pattern)
5. Map-Modus (Choropleth via Leaflet + GeoJSON)
6. Chart-Modus (Chart.js via CDN, Time-Series mit Confidence-Band)
7. `methodik-career.html` (kann unabhängig parallel)
8. i18n + Permalink + QA

Aufwands-Indikation aus MVP-Doku: **~15 Sessions** vom Konzept-Ende zum Live-Dashboard.

### Alternative: Restbereiche Pre-Release abräumen

Wenn lieber Sprint-Ende vor Career-Dashboard-Start:
- Body-Content-Restprüfung 4 statische Pages (1–2 Sessions)
- map.js Radar-Marker-Popup-Refinement (1 Session)
- preliminary-Daten in labor-impact-rates.json verifizieren (1 Session, braucht Zugang zu aktuellen Report-PDFs)
- Architektur-Entscheidung Meta-Tags / hreflang treffen → falls (b), eigene `index.en.html` bauen

---

## Projekt-Konventionen (Reminder für nächste Session)

- Pure HTML/CSS/JS, kein Build-Step im Browser, kein npm
- UI und Planungs-Docs **deutsch**, Code-Kommentare englisch
- LICENSE = AGPL-3.0; Daten = CC BY-NC-SA 4.0
- IIFE-Module-Pattern für JS (`const X = (function(){...})()`)
- Cache-busting via `?v=` auf CSS/JS, `Date.now()` auf Daten
- Deploy = push zu `main` → GitHub Pages automatisch
- Konzept-konsistent arbeiten: Bandbreiten statt Einzelwerte, Quellen sichtbar, neutral-deskriptive Voice
- **NEU 2026-05-01:** i18n-Vollabdeckung Pflicht (`data-i18n-aria`/`-title`/`-placeholder` für Nicht-Body-Strings), Umlaute korrekt schreiben (Details in CLAUDE.md)
- **Flow-Pattern:** Commit auf branch → `git -C <main-worktree> merge --ff-only <branch>` → `git push origin main`. Branch und Worktree am Session-Ende über Harness aufräumen lassen.

---

## Context-Files für Quick-Start in der nächsten Session

In dieser Reihenfolge lesen:

1. `CLAUDE.md` (Wurzel) — Tech-Stack, Patterns, Common Tasks, **die zwei neuen Konventionen**
2. `docs/planung/inventar-index.md` — alle Items 1–103 mit Quelldokument-Links
3. **Diese Übergabe** — Sessions-Stand und Roadmap
4. `docs/planung/pre-release-todos.md` — konkrete Audit-Specs und Restbereiche
5. Wenn Career Dashboard: `docs/planung/career-dashboard-mvp-scope.md` (8-Schritt-Plan, Akzeptanz-Kriterien)

---

## Memory-Files beim User

`~/.claude/projects/C--Users-meyer-Dropbox--6-Block-X-Claude-LF11EA-Iron-Hawk-AIStrikeMap/memory/`

Eintrag dieser Session: `session_2026-05-01_career_konzept_und_item98.md` (referenziert in `MEMORY.md`).

---

## TL;DR — Ein Satz für Eilige

23 Commits in einer Session: Konzeptphase Career Dashboard durchgezogen, Item 98 Labor-Ticker live, i18n strukturell behoben (statisch + dynamisch), zwei Konventionen verankert — nächster Default-Schritt = Career Dashboard MVP Step 1 (Bundle-Script-Skelett), alternativ Pre-Release-Aufräumen.

---

*Dokument erstellt: 2026-05-01 (initial mit Stand `329086f`, finalisiert mit Stand `b0daff9`)*
*Vorgänger-Übergabe: 2026-04-21 (Phase 2 V2 Link-Checker)*
